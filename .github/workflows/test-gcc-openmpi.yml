# Subset CI: GNU + OpenMPI (CPU)
# Quick ECT validation with 4 MPI ranks.

name: "GNU+OpenMPI (CPU)"

on:
  workflow_dispatch:

jobs:
  test:
    uses: ./.github/workflows/_test-compiler.yml
    with:
      compiler: gcc
      mpi: openmpi

